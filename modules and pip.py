import useful_tools

print(useful_tools.roll_dice(20))

# docs.python.org for modules

# pip is package manager, installed packages go in external libraries -> site-packages
# pre-installed modules in external libraries -> lib
